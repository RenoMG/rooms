[project]
name = "rooms"
version = "0.1.0"
description = "A text based adventure backrooms game. Will you find a way out? It's horribly dark in here..."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
